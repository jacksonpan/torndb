[tox]
envlist = py33,py32,py31

[testenv]
# no actual test suite yet but this will at least make sure the
# installation worked and the module is importable.
commands = python -m torndb
changedir = {toxworkdir}
deps = mysql.connector
